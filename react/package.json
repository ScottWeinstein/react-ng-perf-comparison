{
  "name": "react-perf-test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "d3-format": "^1.2.0",
    "lodash": "^4.17.4",
    "react": "^15.5.4",
    "react-dom": "^15.5.4",
    "rxjs": "^5.4.0"
  },
  "devDependencies": {
    "mv": "^2.1.1",
    "react-scripts": "0.9.5",
    "rimraf": "^2.6.1"
  },
  "scripts": {
    "start": "PORT=9000 react-scripts start",
    "build": "react-scripts build",
    "build:gh": "npm run clean:docs-react && react-scripts build && npm run mv -- -v build/* ../docs/react/",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "rimraf": "rimraf",
    "clean:docs-react": "npm run rimraf -- ../docs/react/*",
    "mv": "mv"
  },
  "homepage": "."
}
